name: OSV-Scanner

# Default branch is "develop"
on:
  pull_request:
  push:
    branches: [ develop ]
  merge_group:

# Declaring default permissions as read only.
permissions: read-all

jobs:
  scan-pr:
    uses: "google/osv-scanner/.github/workflows/osv-scanner-reusable-pr.yml@6f0d4ee3c83d2cd7269867f23e5d196de0b146de"
